
# LegendPosition

`tb_pe_client.models.LegendPosition`

## Enum Values


* `TOP` (value: `'top'`)

* `BOTTOM` (value: `'bottom'`)

* `LEFT` (value: `'left'`)

* `RIGHT` (value: `'right'`)



---

### Conventions

- **Package:** `tb_pe_client.models`
- **Attribute access:** `obj.`, `obj.name`, etc.
- **Serialize:** `obj.model_dump()` or `obj.model_dump(by_alias=True)` for camelCase JSON
- **Deserialize:** `LegendPosition.model_validate(data)` or `LegendPosition.model_validate_json(json_str)`
- **None fields:** Optional attributes default to `None`; accessing them never raises exceptions

