
# TimeSeriesChartNoAggregationBarWidthStrategy

`tb_pe_client.models.TimeSeriesChartNoAggregationBarWidthStrategy`

## Enum Values


* `GROUP` (value: `'group'`)

* `SEPARATE` (value: `'separate'`)



---

### Conventions

- **Package:** `tb_pe_client.models`
- **Attribute access:** `obj.`, `obj.name`, etc.
- **Serialize:** `obj.model_dump()` or `obj.model_dump(by_alias=True)` for camelCase JSON
- **Deserialize:** `TimeSeriesChartNoAggregationBarWidthStrategy.model_validate(data)` or `TimeSeriesChartNoAggregationBarWidthStrategy.model_validate_json(json_str)`
- **None fields:** Optional attributes default to `None`; accessing them never raises exceptions

